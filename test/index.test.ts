import { Supra } from '../src';

describe('should', () => {
  it('export 1', async () => {
    // const bot = new Supra({ genericSleepTime: 500 });
    // try {
    //   const results = await bot.search('ES8U');
    //   console.log(results);
    //   expect(results.carMake).toBe("ROLLS ROYCE / CULLINAN V12 AUTO");
    // } finally {
    //   bot.close();
    // }
  })

})
